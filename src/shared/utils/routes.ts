const ROUTES = {
  PROFILE: (id: string) => `/post/${id}`,
  BLOG: () => `/posts`,
  //   REPOSITORY: (owner: string, repository: string) => `/repositories/${owner}/${repository}`,
};

export { ROUTES };
